# ═══════════════════════════════════════════════════════════════════
# Déploiement automatique du site sur GitHub Pages.
# → Chaque modification poussée sur la branche main met le site à jour
#   (~1 minute). Au premier lancement, ce workflow active aussi
#   GitHub Pages automatiquement : aucun réglage manuel nécessaire.
# → Suivi des déploiements : onglet « Actions » du dépôt GitHub.
# ═══════════════════════════════════════════════════════════════════
name: Déployer sur GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Récupérer le code
        uses: actions/checkout@v4

      - name: Configurer GitHub Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Empaqueter le site
        uses: actions/upload-pages-artifact@v3
        with:
          path: .

      - name: Déployer
        id: deployment
        uses: actions/deploy-pages@v4
